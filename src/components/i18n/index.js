import { fromClass } from 'karet';
import * as RI from 'react-intl';

export const Index = fromClass(RI.FormattedMessage);
export const FormattedDate = fromClass(RI.FormattedDate);
export const FormattedNumber = fromClass(RI.FormattedNumber);
export const FormattedTime = fromClass(RI.FormattedTime);
export const FormattedRelative = fromClass(RI.FormattedRelative);
export const FormattedPlural = fromClass(RI.FormattedPlural);
